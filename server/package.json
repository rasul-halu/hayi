{
  "name": "hayi-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend API scaffold for Hayi.",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "postinstall": "prisma generate",
    "test:telegram-auth": "node scripts/generateTestInitData.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "prisma:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^2.2.0",
    "pg": "^8.22.0"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
